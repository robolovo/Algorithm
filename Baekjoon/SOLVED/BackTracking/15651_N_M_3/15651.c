#include <stdio.h>
#include <stdbool.h>

bool visited[8] = { false, };
int	nums[8];

void	dfs(int count, int N, int M)
{
	
}

int	main(void)
{
	int N, M;

	scanf("%d %d", &N, &M);

	dfs(0, N, M);
	return (0);
}
